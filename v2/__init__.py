"""
HYPE Advisor v2 — переработанная архитектура крипто-ассистента.

Отличия от v1 (src/):
  * полностью асинхронный ввод/вывод (httpx + WebSocket вместо REST-опроса),
  * трёхуровневый сканер (быстрый → скам-фильтр → ончейн),
  * динамические стоп/цель от ATR, а не фиксированные проценты,
  * модули разделены: data / analysis / scanner / ai / executor / reporter,
  * наблюдаемость: структурированные логи, счётчик ошибок, супервизор задач,
  * все фильтры включаются/выключаются через .env (v2/config.py).

Точка входа: ``python -m v2 scan`` / ``python -m v2 analyze 0x...``.
"""

__version__ = "2.0.0"
__all__ = ["__version__"]
